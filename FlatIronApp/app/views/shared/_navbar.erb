
<nav style="" class="navbar navbar-expand-lg navbar-dark bg-dark">
  <h5 class="navbar-brand" style="color:white;font-size: 1.1em;padding-top:13px;">Making Friends One Line of Code at a Time</h5>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/users">All Profiles <span class="sr-only">(current)</span></a>
      </li>
      <!-- <li class="nav-item">
        <a class="nav-link" href="/users/<%= current_user.id if current_user %>"> View Profile</a>
      </li> -->
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Actions
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="/users/<%= current_user.id if current_user %>"> View Profile</a>
          <a class="dropdown-item" href="/users/<%= current_user.id if current_user %>/edit">Edit Profile</a>
            <a class="dropdown-item" href="/sent_requests/new">Send a Request</a>
              <a class="dropdown-item" href="/matches/new">Guess Your Match</a>
          <div class="dropdown-divider"></div>
          <%  if current_user %>
            <%= form_tag user_path(current_user), method: "delete", :class => 'dropdown-item'  do %>
            <%= submit_tag "Delete Profile" %>
            <% end %>
            <%= form_tag "/logout", method: :delete, :class => 'dropdown-item' do %>
            <%= submit_tag "Logout" %>
            <% end %>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>
